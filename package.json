{
  "name": "timer-pwa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "零依赖计时器 PWA：正计时秒表 + 多路倒计时提醒（响铃、震动、系统通知）。",
  "scripts": {
    "test": "node --experimental-test-isolation=none --test \"tests/*.test.js\"",
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "icons": "node scripts/gen-icons.mjs"
  },
  "engines": {
    "node": ">=22.8"
  }
}
